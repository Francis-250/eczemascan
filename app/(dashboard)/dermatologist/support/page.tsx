import DermatologistSupport from "@/components/dermatologist/support";

export default async function DermatologistSupportPage() {
  // Dermatologists can view their assigned tickets
  // For now, we'll show a placeholder
  return <DermatologistSupport />;
}